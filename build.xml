<?xml version="1.0" encoding="UTF-8"?>
<project name="Cache" default="default" basedir=".">
  <description>Builds, tests, and runs the project Cache.</description>
  <import file="nbproject/build-impl.xml"/>

  <!-- <target name="-post-jar">
    <jar jarfile="dist/Cache.jar" excludes="META-INF/*">
      <zipgroupfileset dir="dist/lib" includes="*.jar" excludes="META-INF/*"/>
      <manifest>
        <attribute name="Main-Class" value="cache.Cache" />
      </manifest>
    </jar>
  </target> -->

</project>
